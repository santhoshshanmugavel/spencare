[build]
  base = "apps/web"
  command = "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @spencare/web build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
